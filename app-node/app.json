{
  "name": "dahlia-web",
  "description": "DAHLIA Housing Portal - Node/TS primary web server (TanStack Start)",
  "keywords": ["node", "tanstack-start", "housing", "dahlia"],
  "repository": "https://github.com/SFDigitalServices/sf-dahlia-web",
  "stack": "heroku-24",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "standard-1x"
    },
    "worker": {
      "quantity": 1,
      "size": "standard-1x"
    }
  },
  "addons": [
    {
      "plan": "heroku-redis:mini",
      "as": "REDIS"
    },
    {
      "plan": "heroku-postgresql:essential-0",
      "as": "DATABASE"
    }
  ],
  "env": {
    "NODE_ENV": {
      "description": "Node environment (production for deployed apps)",
      "value": "production"
    },
    "RAILS_API_BASE_URL": {
      "description": "Internal URL of the Rails Salesforce proxy Heroku app (e.g., https://dahlia-salesforce-proxy.herokuapp.com)",
      "required": true
    },
    "INTERNAL_API_KEY": {
      "description": "Shared secret for Node→Rails proxy authentication",
      "required": true
    },
    "CLERK_SECRET_KEY": {
      "description": "Clerk secret key for server-side auth",
      "required": true
    },
    "CLERK_PUBLISHABLE_KEY": {
      "description": "Clerk publishable key for client-side auth",
      "required": true
    },
    "DATABASE_URL": {
      "description": "PostgreSQL connection URL (auto-set by Heroku add-on)",
      "required": true
    },
    "REDIS_URL": {
      "description": "Redis connection URL (auto-set by Heroku add-on)",
      "required": true
    },
    "CACHE_WARM_ENABLED": {
      "description": "Enable the scheduled Redis cache pre-warm on the worker dyno (\"true\"/\"1\"). See docs/cache-prewarm-plan.md.",
      "required": false,
      "value": "false"
    },
    "CACHE_WARM_INTERVAL_MS": {
      "description": "Cache pre-warm repeat cadence in ms; must stay below the listing TTLs (1 day). Defaults to 6h.",
      "required": false
    },
    "CACHE_WARM_CONCURRENCY": {
      "description": "Max listings warmed in parallel per pass. Defaults to 4.",
      "required": false
    }
  },
  "scripts": {
    "postdeploy": "npm run db:migrate"
  },
  "environments": {
    "production": {
      "env": {
        "NODE_ENV": "production"
      }
    },
    "staging": {
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
